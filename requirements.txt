# Google Cloud dependencies - pin grpcio first to avoid slow downloads
grpcio>=1.60.0,<1.70.0

# Firebase (google-auth and google-cloud-storage are auto-installed as dependencies if needed)
firebase-admin>=6.2.0
google-cloud-firestore>=2.11.1

# Machine Learning Enhancements
tensorflow>=2.10.0
# keras comes bundled with tensorflow, no separate package needed

# PyTorch and YOLOv5
torch>=1.9.0
torchvision>=0.10.0  # Required by ultralytics
ultralytics>=8.0.0

# Image processing
opencv-python>=4.5.0
Pillow>=8.0.0
numpy>=1.21.0

# ML utilities
scikit-learn>=1.0.0
scipy>=1.7.0
dlib>=19.22.0

# Face recognition
face-recognition>=1.3.0

# Web framework
flask>=2.0.0
